
/*function main() {
    const hasVoted = document.querySelector('#hasVoted').value;
    const elections = document.querySelector('#elections').value;
    for (let i = 0; i < hasVoted.length; i++) {
        if (hasVoted[i]) {
            const voteForm = document.getElementById(elections[i]._id);
            console.log(elections[i]);
            voteForm.parentNode.removeChild(voteForm);
        }
    }
}

document.addEventListener('DOMContentLoaded', main);*/